"""
 - Improve error handling in `decode_element()` and at `main()`, lines 62-65 and 78

 - Add comments to `decode_elements()`

 - Exit confirmation

 - Reloading (with `<r>`)

 - Debug console (with `<d>`

 - Add more elements (like `para`, `line`, `nl` etc. )
"""